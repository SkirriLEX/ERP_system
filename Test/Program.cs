using System;

namespace Test
{
    class Program
    {
        static void Main(string[] args)
        {
            var pos = new Person();
            //pos.GetPerson();
            var str = "35454";
            pos.InsertPerson(1, str, str, str, DateTime.Today, 3,1,str, 254354425,str, 
              DateTime.Today, DateTime.Today);
        }
    }
}
